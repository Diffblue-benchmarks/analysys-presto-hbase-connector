buildCmd: mvn -e -X -Dmaven.test.skip=true compile
testCmd: true
lineLength: 4096
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
# Target TGF: 148.5
#  >methods=2
#  >complexity=33
#  >construction=9.0
#  >dependency=0.5
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/utils/Types.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/utils/Types.java
# Target TGF: 121.0375
#  >methods=17
#  >complexity=573
#  >construction=7.0
#  >dependency=5.45
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/query/HBaseRecordCursor.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/query/HBaseRecordCursor.java
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/HBaseColumnHandle.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/HBaseColumnHandle.java
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/utils/Utils.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/utils/Utils.java
# Target TGF: 889.125
#  >methods=9
#  >complexity=294
#  >construction=31.5
#  >dependency=3.5
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/utils/Utils.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/utils/Utils.java
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/HBaseColumnMetadata.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/HBaseColumnMetadata.java
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/schedule/ConditionInfo.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/schedule/ConditionInfo.java
# Target TGF: 53.0
#  >methods=5
#  >complexity=53
#  >construction=3.0
#  >dependency=3.0
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/HBaseTableHandle.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/HBaseTableHandle.java
# Target TGF: 115.0
#  >methods=7
#  >complexity=115
#  >construction=2.0
#  >dependency=2.0
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/HBaseColumnMetadata.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/HBaseColumnMetadata.java
# Target TGF: 105.5
#  >methods=19
#  >complexity=139
#  >construction=5.0
#  >dependency=3.83333333333
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/TableMetaInfo.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/TableMetaInfo.java
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/ColumnMetaInfo.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/ColumnMetaInfo.java
# Target TGF: 95.5
#  >methods=11
#  >complexity=106
#  >construction=3.5
#  >dependency=4.0
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/HBaseColumnHandle.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/HBaseColumnHandle.java
# Target TGF: 204.666666667
#  >methods=11
#  >complexity=345
#  >construction=5.0
#  >dependency=3.83333333333
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/schedule/ConditionInfo.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/schedule/ConditionInfo.java
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/utils/Constant$CONDITION_OPER.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/utils/Constant.java
# Target TGF: 67.0
#  >methods=22
#  >complexity=97
#  >construction=2.5
#  >dependency=3.33333333333
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/meta/HBaseConfig.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/meta/HBaseConfig.java
# Target TGF: 83.0
#  >methods=13
#  >complexity=83
#  >construction=3.0
#  >dependency=3.0
 - target:
   - dir: target/classes
     file: com/analysys/presto/connector/hbase/schedule/HBaseSplit.class
     sourceDir: src/main/java
     sourceFile: com/analysys/presto/connector/hbase/schedule/HBaseSplit.java
